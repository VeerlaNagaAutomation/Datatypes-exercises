How to print class name of class

class Test:
    pass

print(Test.__name__) # Test
#######
by useing object:
class Test:
    pass

obj = Test()
print(obj.__class__.__name__) # Test

__class__ gives the class of the object, and __name__ gives the class name.

############
✅ 3. Using type() (Very common in interviews)
class Test:
    pass

obj = Test()
print(type(obj).__name__) #Test


##########################################################
✅ 2️⃣ Duck Typing (Very Simple)

👉 Python cares about method, not class

class Dog:
    def sound(self):
        print("Bark")

class Cat:
    def sound(self):
        print("Meow")

def make_sound(animal):
    animal.sound()

make_sound(Dog())
make_sound(Cat())

✅ Output
Bark
Meow

🧠 One-line Explanation

If an object has the required method, Python uses it—class type doesn’t matter.



########################################################
✅ Correct Way to Use super() (VERY IMPORTANT)
✔ Call super() inside the overridden method
class Parent:
    def show(self):
        print("This is Parent")

class Child(Parent):
    def show(self):
        super().show()        # call parent method
        print("This is Child")

obj = Child()
obj.show()

✅ Output
This is Parent
This is Child

🧠 Simple Explanation (Interview Style)

super() is used inside a child class method to call the parent class implementation of the same method.

🔑 Key Rules of super() (Memorize)

❌ Cannot be used outside class

✅ Must be inside a child method

✅ Used to call parent method

✅ Avoids hardcoding parent class name

❌ Wrong vs ✅ Right (Quick Compare)

❌ Wrong

super().show()


✅ Right

def show(self):
    super().show()

🎯 One-Line Interview Answer

super() is used inside a child class method to access parent class methods and supports method overriding in polymorphism.


##########################
Below is a clear, simple, interview-ready explanation of super() with Multiple Inheritance and MRO (Method Resolution Order).
I’ll keep it easy, with small code and step-by-step logic.

🔹 super() with Multiple Inheritance
📌 Why super() matters here

In multiple inheritance, more than one parent exists.
super() ensures:

Each class method is called once

Calls follow a well-defined order (MRO)

Avoids duplicate or skipped method calls

✅ Basic Multiple Inheritance Example
class A:
    def show(self):
        print("A")

class B(A):
    def show(self):
        super().show()
        print("B")

class C(A):
    def show(self):
        super().show()
        print("C")

class D(B, C):
    def show(self):
        super().show()
        print("D")

obj = D()
obj.show()

✅ Output
A
C
B
D


👀 Order looks surprising?
That’s where MRO comes in.

🔹 What is MRO (Method Resolution Order)?
📌 Definition (Interview Line)

MRO is the order in which Python searches for a method in a class hierarchy.
#########################
🔹 Diamond Problem (Classic Interview Topic)
     A
    / \
   B   C
    \ /
     D


Without super() → A may run twice
With super() → A runs once ✔️

✅ Correct Diamond Pattern
class A:
    def show(self):
        print("A")

class B(A):
    def show(self):
        super().show()
        print("B")

class C(A):
    def show(self):
        super().show()
        print("C")

class D(B, C):
    def show(self):
        super().show()
        print("D")

D().show()

🔑 Rules to Remember (Interview Gold)

super() follows MRO

MRO is left-to-right, depth-first

Each method executes once

Always use super() in multiple inheritance

Use .mro() to debug issues

🎯 Perfect Interview Answer (Memorize)

In multiple inheritance, super() follows the Method Resolution Order (MRO) to ensure each parent method is called once. Python uses C3 linearization to determine this order, preventing duplicate calls and resolving the diamond problem.
#################################
✅ Dunder Functions — Interview Definition (Simple & Crisp)

Dunder (double underscore) functions are special methods in Python that start and end with __ and are automatically invoked by Python to define or customize the behavior of objects with built-in operations like object creation, printing, comparison, and arithmetic.

# Dunder functions
# 4. __add__ → Addition (+)
class Number:
    def __init__(self,x):
        self.x = x
    
    def __add__(self,other):
        return self.x + other.x
a = Number(10)
b = Number(20)
print(a+b)  # 30

# 5️⃣ __eq__ → Equality (==)

class Test:
    def __init__(self,x):
        self.x = x
    def __eq__(self,other):
        return self.x == other.x
        
a = Test(5)
b = Test(5)

print(a==b) # True

6️⃣ __lt__ → Less than (<)

Used for comparisons.

class Test:
    def __init__(self, x):
        self.x = x

    def __lt__(self, other):
        return self.x < other.x

print(Test(3) < Test(5))


7️⃣ __del__ → Destructor

Called when object is deleted.

class Demo:
    def __del__(self):
        print("Object destroyed")

obj = Demo()
del obj


2️⃣ __str__ → String representation

Called when you use print(object).

class Person:
    def __init__(self, name):
        self.name = name

    def __str__(self):
        return self.name

p = Person("Ram")
print(p) #Ram


3️⃣ __len__ → Length

Called when len() is used.

class MyList:
    def __len__(self):
        return 5

obj = MyList()
print(len(obj)) #5

####################################
